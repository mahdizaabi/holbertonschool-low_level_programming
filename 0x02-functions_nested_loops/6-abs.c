#include "holberton.h"

/**
 * _abs - Computing the absolut value of an given number.
 * @n: The variable Input
 *
 * Return: n if n positive,
 *         -n if n negative,
 *
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
